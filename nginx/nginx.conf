# nginx/nginx.conf
# Reverse proxy (dev): single HTTPS entry point. Listens on port 443 inside the
# container, which compose publishes on HTTPS_PORT (default 8443).
#   :8443       -> nginx reverse proxy
#   :8443/api/*      -> NestJS backend (the /api prefix is stripped, see trailing slash)
#   :8443/socket.io/ -> backend (real-time WebSocket, once the gateway exists)
#   :8443/*          -> Vite dev server (front + HMR)
#   :5555            -> Prisma Studio
#   :5601            -> Kibana
#   :9200            -> Elasticsearch

worker_processes auto;
events { worker_connections 1024; }

http {
  include       /etc/nginx/mime.types;
  default_type  application/octet-stream;
  resolver      10.89.0.1 valid=10s;

  map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
  }

  server {
    access_log off;
    listen 443 ssl;
    server_name localhost;

    ssl_certificate     /etc/nginx/certs/cert.pem;
    ssl_certificate_key /etc/nginx/certs/key.pem;

    # -- REST API -> backend ---------------------------------
    # trailing slash on proxy_pass => /api/health becomes /health on the backend
    location /api/ {
      proxy_pass http://backend:3000/;
      proxy_http_version 1.1;
      proxy_set_header Host              $host;
      proxy_set_header X-Real-IP         $remote_addr;
      proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
      proxy_set_header Upgrade           $http_upgrade;
      proxy_set_header Connection        $connection_upgrade;
    }

    # -- Real-time WebSocket -> backend ----------------------
    location /socket.io/ {
      proxy_pass http://backend:3000;
      proxy_http_version 1.1;
      proxy_set_header Host       $host;
      proxy_set_header Upgrade    $http_upgrade;
      proxy_set_header Connection $connection_upgrade;
      proxy_read_timeout 3600s;
    }

    # -- Front (Vite dev server + HMR over wss) -> frontend --
    location / {
      proxy_pass http://frontend:5173;
      proxy_http_version 1.1;
      proxy_set_header Host       $host;
      proxy_set_header Upgrade    $http_upgrade;   # required for hot-reload (HMR)
      proxy_set_header Connection $connection_upgrade;
      proxy_read_timeout 3600s;
    }
  }
}
