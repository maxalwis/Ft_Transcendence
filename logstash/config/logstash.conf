input {
  tcp {
    port => 5044
    codec => json
  }
}

filter {
  # Optional parsing/enrichment can go here
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "nestjs-logs-%{+YYYY.MM.dd}"
  }
}